
const initialState = {
    numOfQuestions: 0,
    timeForGame: 0    
}

const questionsReducer = (state = initialState , action = {} ) => {
   return initialState;
}

export default questionsReducer;